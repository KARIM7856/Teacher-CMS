<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Streaming video, fetching PDFs, and reaching Supabase all need network. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Store-readiness attributes on <application> below, in order:
         • supportsRtl   — the whole UI is right-to-left. This mirrors platform
           surfaces too (the WebView used for Drive/YouTube, PDF rendering, system
           dialogs), not just the Flutter layer, which sets its own direction.
         • usesCleartextTraffic="false" — everything the app talks to is HTTPS
           (Supabase, storage, embeds). Stating it explicitly means an accidental
           http:// media link fails loudly instead of quietly sending a student's
           traffic unencrypted.
         • allowBackup="false" + dataExtractionRules — the Supabase session token
           lives in shared_preferences; Android Auto Backup and device-to-device
           transfer would copy a signed-in session onto another device. Opt out of
           both (allowBackup covers pre-12, the xml rules cover Android 12+).
           Nothing local is worth restoring — all content is server-side. -->
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher needs to see a browser to open external links (Android 11+). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- ...and a mail client, for the account-deletion request link. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
